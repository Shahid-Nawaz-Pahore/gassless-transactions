import { encodeAbiParameters } from 'viem';
import { ACCOUNT_LOCKER_HOOK, ACCOUNT_LOCKER_SOURCE_EXECUTOR, ACCOUNT_LOCKER_TARGET_EXECUTOR, } from './constants.js';
export const getAccountLockerHook = ({ isOmniMode, hook } = {
    isOmniMode: false,
}) => {
    return {
        module: ACCOUNT_LOCKER_HOOK,
        address: ACCOUNT_LOCKER_HOOK,
        initData: encodeAbiParameters([{ name: 'enableOmniLock', type: 'bool' }], [isOmniMode]),
        deInitData: '0x',
        additionalContext: '0x',
        hook,
        type: 'hook',
    };
};
export const getAccountLockerSourceExecutor = ({ hook, } = {}) => {
    return {
        module: ACCOUNT_LOCKER_SOURCE_EXECUTOR,
        address: ACCOUNT_LOCKER_SOURCE_EXECUTOR,
        initData: '0x',
        deInitData: '0x',
        additionalContext: '0x',
        hook,
        type: 'executor',
    };
};
export const getAccountLockerTargetExecutor = ({ hook, moduleType, } = { moduleType: 'executor' }) => {
    return {
        module: ACCOUNT_LOCKER_TARGET_EXECUTOR,
        address: ACCOUNT_LOCKER_TARGET_EXECUTOR,
        initData: '0x',
        deInitData: '0x',
        additionalContext: '0x',
        hook,
        type: moduleType,
    };
};
//# sourceMappingURL=installation.js.map